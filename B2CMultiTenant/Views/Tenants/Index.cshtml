@model Dictionary<int, string>

@{
    ViewBag.Title = "Tenants";
}

<h2>Tenants</h2>

<p>
    @Html.ActionLink("New tenant", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.Display("Tenant name")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Value, "Select", new { id = item.Key })
            @*@Html.DisplayFor(modelItem => item.Value)*@
        </td>
        @*<td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Key }) |
            @Html.ActionLink("Details", "Details", new { id=item.Key }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Key })
        </td>*@
    </tr>
}

</table>
